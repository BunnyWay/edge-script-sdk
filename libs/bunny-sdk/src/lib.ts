import * as net from "./net/index.ts";
import * as cache from "./cache/index.ts";

export {
  /**
   * The `cache` module contains Cache related functions and utilities.
   */
  cache,
  /**
   * The `net` module contains Network related functions and utilities.
   */
  net,
};
